Weather application, using node.js and react, styling with css

To run the project:
    first terminal:
cd <your_directory>
{
    cd Backend
    npm install
    node server.js
}
    second terminal:
cd <your_directory>
{
    cd Frontend
    npm install
    npm run dev
}
